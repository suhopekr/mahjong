# 마작 솔리테어 웹게임 — 개발 스펙 v1

이 문서는 Claude Code에게 전달하는 빌드 명세다. 아래 스펙대로 정적 웹사이트 형태의 마작 솔리테어 게임을 처음부터 구현하라.

## 0. 프로젝트 배경 (설계 판단 기준)

- 타겟 사용자: 미국의 50세 이상 캐주얼 게이머. 스마트폰·태블릿·데스크톱 브라우저에서 플레이.
- 비즈니스 모델: 검색 유입(SEO) 기반 트래픽 → 광고 수익. 경쟁 상위 사이트들은 페이지 1~2개짜리 초경량 사이트이므로, 속도와 단순함이 곧 경쟁력이다.
- 모든 설계 판단에서 우선순위: (1) 로딩 속도 (2) 시니어 사용성 (3) SEO (4) 코드 확장성.

## 1. 기술 제약

- 순수 HTML + CSS + 바닐라 JavaScript. 프레임워크·빌드 도구 금지 (React, 번들러, 트랜스파일러 없음).
- 파일 구성: `index.html`, `style.css`, `game.js` 3파일 기본. 필요시 `tiles.js`(타일셋 데이터) 분리 허용. 외부 라이브러리·CDN 의존성 0개.
- 타일 렌더링은 인라인 SVG 또는 CSS로 그린다. 이미지 파일(png/jpg) 사용 금지 — 로딩 속도와 확대 선명도 때문.
- 정적 호스팅(Vercel/Cloudflare Pages) 배포 가능해야 함. 서버 코드 없음.
- 브라우저 지원: 최신 Chrome/Safari/Edge/Firefox + iOS Safari, Android Chrome. IE 미지원.
- 전체 페이지 초기 로드(HTML+CSS+JS 합계)는 gzip 기준 150KB 이하 목표.

## 2. 게임 규칙 (표준 마작 솔리테어)

- 타일 144개: 일반 타일 34종 × 4개(=136) + 꽃(Flower) 4개 + 계절(Season) 4개.
  - 일반 34종: 통(Dots) 1–9, 삭(Bamboo) 1–9, 만(Characters) 1–9, 바람(East/South/West/North) 4종, 용(Red/Green/White) 3종.
- 매칭 규칙: 동일한 타일 2개를 선택하면 제거. 예외 — 꽃 4개는 서로 아무거나 매칭 가능, 계절 4개도 서로 아무거나 매칭 가능.
- "열린(free) 타일" 판정: 위에 다른 타일이 (부분적으로라도) 올려져 있지 않고, 같은 층에서 왼쪽 또는 오른쪽 중 한쪽이 비어 있어야 선택 가능.
- 레이아웃: v1은 클래식 거북이(Turtle) 배치 1종. 좌표계는 (x, y, z) 그리드로 정의하되 타일이 반 칸(0.5 단위) 겹칠 수 있는 표준 방식 사용. 레이아웃 데이터는 좌표 배열로 분리 정의해서 추후 레이아웃 추가가 데이터 추가만으로 가능하게 할 것.

## 3. 보드 생성 — 반드시 풀 수 있는 판 (핵심 요구사항)

무작위 배치는 금지. 다음 역순 생성 알고리즘으로 100% 풀 수 있는 판을 보장하라:

1. 레이아웃의 모든 좌표 슬롯을 비운 상태에서 시작한다.
2. "제거의 역순 = 배치"라는 원리로, 매 단계에서 현재 배치 상태 기준으로 배치 가능한(=게임 중이라면 제거 가능했을) 슬롯 중 2개를 고른다.
3. 타일 쌍(동일 타일 2개, 또는 꽃/계절 쌍)을 그 두 슬롯에 배치한다.
4. 144개가 모두 배치될 때까지 반복. 막다른 상태(배치 가능한 슬롯 조합이 없음)에 도달하면 백트래킹하거나 처음부터 재시도.
5. 생성 시간은 일반 기기에서 200ms 이내여야 한다. 필요시 재시도 상한 + 시드 조정.

셔플 기능도 동일 원리 적용: 남은 타일들을 현재 남은 슬롯에 대해 역순 생성으로 재배치해서 셔플 후에도 풀 수 있는 상태를 보장하라. 재배치가 불가능한 구성이면 "가능한 최선 배치 + 사용자에게 새 게임 권유" 처리.

## 4. 게임 기능

- 조작: 클릭(탭) 두 번 — 첫 탭 선택(강조 표시), 같은 짝 탭하면 제거. 다른 타일 탭하면 선택 이동. 드래그 조작 없음.
- 되돌리기(Undo): 무제한. 전체 이동 스택 유지. 셔플도 되돌리기 가능하면 포함, 어려우면 셔플 시 스택 초기화하고 사용자에게 고지.
- 힌트(Hint): 현재 열린 타일 중 매칭 가능한 쌍 하나를 3회 깜빡여 표시. 사용 횟수 무제한.
- 남은 매칭 수 표시: 화면 구석에 "가능한 짝: N" 상시 표시. N=0이 되면 모달로 "더 이상 짝이 없습니다 — 섞기 / 되돌리기 / 새 게임" 3버튼 제시.
- 승리: 모든 타일 제거 시 절제된 축하 화면(과한 애니메이션 금지) + 플레이 시간 표시 + "새 게임" 버튼.
- 타이머: 조용히 카운트만 하고 화면에는 작게 표시. 시간 압박 연출(빨간색, 카운트다운 등) 절대 금지. 설정에서 타이머 숨김 가능.
- 일시정지 개념 없음 — 시간 제한이 없으므로 불필요.

## 5. 시니어 친화 UI 스펙 (차별화 핵심)

- 터치 타겟 최소 48×48px. 타일은 화면에서 실측 그 이상.
- 타일 크기: 설정에 3단계(보통/크게/아주 크게) 토글. 기본값 "크게". 크기 변경 시 보드가 화면에 맞게 스케일되고 필요시 스크롤 허용.
- 타일셋 2종 토글:
  - 클래식: 전통 마작 문양 SVG.
  - 큰 글씨(기본값): 한자·세밀한 문양을 큰 숫자 + 단순 아이콘 + 색상 코딩(통=파랑, 삭=초록, 만=주황 등)으로 대체. 노안·저시력에서 구분 최우선. 같은 종류는 두 타일셋에서 동일 매칭 로직.
- 열린 타일 표시 토글(기본 켬): 선택 가능한 타일을 미묘한 밝기 차이로 구분, 잠긴 타일은 살짝 어둡게.
- 화면 상시 버튼 4개만: 새 게임, 되돌리기, 힌트, 전체화면(Fullscreen API). 그 외(타일 크기, 타일셋, 소리, 열린 타일 표시, 타이머 표시)는 톱니바퀴 아이콘 설정 패널에 수납.
- 버튼은 텍스트 라벨 병기(아이콘만 금지). 폰트 최소 18px, 버튼 라벨 16px 이상.
- 색 대비 WCAG AA 이상. 배경은 차분한 단색/미묘한 그라데이션(초록 펠트톤 권장).
- 애니메이션: 타일 제거 시 0.15s 페이드 정도만. `prefers-reduced-motion` 존중.
- 소리: 기본 꺼짐. 켜면 부드러운 클릭/매칭음만 (Web Audio로 생성, 오디오 파일 없이).
- 오배치 방지: 게임 보드와 버튼 주변 광고 배치 금지 영역 확보 (아래 8절).

## 6. 저장 (localStorage)

- 자동 저장: 매 수마다 보드 상태·이동 스택·경과 시간 저장. 페이지 재방문 시 "이어하기 / 새 게임" 선택 제시.
- 설정 저장: 타일 크기, 타일셋, 소리, 표시 옵션.
- 통계 저장: 총 게임 수, 승리 수, 최단 클리어 시간. 설정 패널에서 열람.
- localStorage 불가 환경(시크릿 모드 등)에서도 게임 자체는 정상 동작해야 함 (try-catch로 감싸기).

## 7. SEO 요구사항

- `<title>`: "Mahjong Solitaire — Free Online Mahjong, Full Screen, No Download"
- H1: "Free Mahjong Solitaire" (게임 위 or 접힌 헤더). 게임이 first view를 차지하고 텍스트는 스크롤 아래.
- 게임 아래 본문 섹션(영어): ① 게임 소개 2~3문단 — "mahjong", "free mahjong", "mahjong solitaire", "full screen", "no download", "no time limit" 키워드 자연스럽게 포함. Windows의 클래식 마작(Titans 스타일)을 기억하는 사용자를 향한 문구 1줄 포함하되 "Titans"를 상표처럼 쓰지 말고 서술적으로만 언급. ② How to Play 룰 설명 ③ FAQ 5~7문항(Is it free? / Do I need to download? / Is there a time limit? / Can I undo? / Does it save my game? 등).
- FAQ에 JSON-LD `FAQPage` 스키마, 게임에 `VideoGame`/`WebApplication` 스키마.
- 메타: description, Open Graph(og:title/description/image — og 이미지는 SVG 보드 스크린샷 기반 정적 1장 허용, 이 1장만 png 예외), canonical, viewport.
- 시맨틱 HTML(header/main/section/footer), 이미지 없는 구조로 LCP는 보드 렌더 자체. Lighthouse 성능 90+ 목표.
- robots.txt, sitemap.xml 포함.

## 8. 광고 슬롯 (v1은 자리만)

- 실제 광고 코드는 넣지 말고 고정 크기 placeholder `<div>`로 예약: 데스크톱 — 우측 300×250 1개, 하단 728×90 1개. 모바일 — 본문 텍스트 사이 320×100 1개.
- 게임 보드·조작 버튼에서 최소 24px 이상 이격. 버튼과 인접 배치 금지.
- 고정 크기 컨테이너로 CLS(레이아웃 이동) 0 유지.
- 코드에 `<!-- AD SLOT: adsense -->` 주석으로 표시해 나중에 삽입 지점 명확화.

## 9. 접근성

- 키보드 조작 지원: Tab으로 열린 타일 순회, Enter로 선택/매칭, U=되돌리기, H=힌트, N=새 게임.
- 타일에 aria-label (예: "Bamboo 3, selectable"). 상태 변화는 aria-live 영역으로 알림.
- 포커스 링 명확히 표시.

## 10. 코드 품질 / 구조

- game.js 내부 모듈 구분: `layout`(좌표 데이터), `board`(생성·상태), `rules`(free 판정·매칭), `render`(DOM/SVG), `ui`(버튼·설정), `storage`. ES 모듈 없이 단일 파일이면 명확한 섹션 주석으로 구분.
- 레이아웃·타일셋은 데이터로 정의해 v2에서 레이아웃 추가/다국어/CrazyGames SDK 빌드 변형이 쉽도록 게임 로직과 분리.
- 보드 생성기·free 판정 로직에 간단한 콘솔 셀프테스트 함수 포함 (`runSelfTest()` — 생성 1,000판 전부 풀이 가능 검증).

## 11. 완료 기준 (체크리스트)

- [ ] 새 게임 1,000회 생성 셀프테스트 전부 통과 (풀 수 있는 판 100%)
- [ ] 모바일(375px 폭)과 데스크톱에서 조작·표시 정상
- [ ] 되돌리기/힌트/셔플/이어하기 동작
- [ ] 큰 글씨 타일셋에서 모든 타일 종류가 육안으로 즉시 구분 가능
- [ ] Lighthouse: Performance 90+, Accessibility 95+, SEO 95+
- [ ] 콘솔 에러 0, 외부 요청 0 (광고 placeholder 제외)

## 12. v1 범위 밖 (구현하지 말 것, 구조만 대비)

- 추가 레이아웃, Mahjong Connect 변형, 다국어, CrazyGames SDK 연동, 계정/리더보드, 데일리 챌린지.
