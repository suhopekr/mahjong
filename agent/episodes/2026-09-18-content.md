task: content (창조자 수동 첫 실행)
idle: false
tier: flat (metrics.csv 28행, 8주 미만 → 기준선 수집 중; pulse-latest 2026-09-18 = 최신)
inputs: pulse-latest.md 2026-08-21..09-17, GSC 2026-08-19..09-15 (노출 178 / 클릭 0), metrics.csv 28행, SOUL Strategy 1
observations:
- 홈(/)이 노출 155/178, 평균 순위 79. 새 게임 페이지는 여전히 노출 0 → Strategy 1은 홈이 유일한 지렛대.
- GSC 쿼리 군집: "classic mahjong full screen (without downloading|no download)" 17회(13+3+1, 최대 단일 군집), "free mahjong solitaire (full screen|no download|online|without downloading)" 약 30회, "free mahjong no download (needed|required|…)" 약 14회.
- 기존 홈 title 81자: "Mahjong Solitaire — Free Online Mahjong, Full Screen, No Download | Easy Classics". SERP 표시 한계(약 60자)에서 "No Download"부터 잘리고, "classic"이라는 단어가 title/description에 없음. description 162자(POLICY 120–155 초과, 끝이 잘림).
- 홈 FAQ는 이미 no-download(Q2)·full screen(Q7)을 담고 있어 FAQ 수정 불필요. FAQ 문안을 바꾸면 /i18n/pages/index.js 13개 언어 번역이 함께 드리프트하므로 이번엔 건드리지 않음.
- tools/site-check.mjs에 "<title> ends with the brand" 검사가 있어 브랜드 접미사는 필수 → POLICY의 title ≤ 60자는 브랜드 포함으로는 어떤 페이지도 못 지킴. 핵심어를 앞 57자 안에 넣는 것으로 대체.
- git HEAD(caccb26)에서 tools/agent/pulse.mjs가 upsert로 바뀜 → P-003 사실상 반영됨(PROPOSALS 결정 줄은 비어 있음).
decisions:
- 홈 title/description/og만 검색 의도에 맞게 재작성 (근거: 위 쿼리 군집, 노출의 87%가 홈). 새 페이지는 만들지 않음 — 노출 있는 페이지의 CTR·순위 개선이 Strategy 1의 첫 단계.
- title: "Free Classic Mahjong Solitaire — Full Screen, No Download | Easy Classics" (73자, 핵심어는 앞 57자). description 155자, "classic"·"full screen"·"no download"·"no sign-up"·"no time limit" 포함.
- 커밋하지 않음: device_request_delete_permission이 거부되어 _common.md 규칙대로 git 쓰기 명령을 실행하지 않음(index.lock 잔류 방지). 변경은 main 작업 트리에 미커밋 상태로 남김.
actions:
- index.html 4줄 변경 (title, meta description, og:title, og:description). `npm --prefix tools test` 전부 통과(site check 396/396). `npm run check`는 Playwright 미설치로 실행 불가(node_modules 없음) → `node tools/site-check.mjs --static` 396 통과로 대체.
- 브랜치·커밋 없음. 창조자용 커밋 메시지 초안:
    Home: align title and description with search intent
    
    GSC 2026-08-19..09-15: 155 of 178 impressions land on /, avg position 79,
    0 clicks. The largest query cluster is "free classic mahjong full screen
    without downloading / no download" (17 impr) followed by "free mahjong
    solitaire full screen / no download" (~30 impr). The old 81-char title
    lost "No Download" past the SERP cutoff and never said "classic"; the
    162-char description was truncated. New title keeps every modifier in
    the first 57 chars; description is 155 chars.
    
    Agent: growth
next:
- 창조자: `git switch -c agent/2026-09-18-home-search-intent`, `git add index.html`, 위 메시지로 커밋 후 push. 다음 실행부터는 삭제 권한 프롬프트를 승인해 주면 에이전트가 직접 브랜치/커밋 가능.
- 3~4주 뒤 펄스에서 홈의 GSC 순위(79)와 CTR 변화 확인. 개선 없으면 다음 후보: /daily.html("daily mahjong challenge" 순위 ~40) description에 'free / no download' 강화, 게임 페이지 title 길이(72–81자) 정리.
- 미해결: 홈 FAQ에 "Is this classic mahjong?" 같은 Q를 추가하려면 index.js 13개 언어 번역이 동반되어야 함 — 다음 스프린트 후보.
